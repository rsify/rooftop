const expect = require('chai').expect
const _ = require('lodash')
const User = require('../models/User')

const c = require('../utils/repl').ctx()

describe('repl commands', () => {
	describe('help', () => {
		// too lazy to stub console.log
	})
	describe('users', () => {
		describe('add', () => {
			it('should add a user and save it', () => {
				// not implemented
			}) 
		})
	})
})